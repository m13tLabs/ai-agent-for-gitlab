# chart-testing values for per-token secretKeyRefs: the GitLab token comes from
# another Secret, the chart Secret generates the webhook secret and admin token.
gitlab:
  aiUsername: review-agent
secrets:
  secretKeyRefs:
    gitlabToken:
      name: gitlab-token
      key: token
