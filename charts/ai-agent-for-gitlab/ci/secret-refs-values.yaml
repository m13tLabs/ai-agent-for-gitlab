# chart-testing values for per-token secretKeyRefs: the GitLab token comes from
# another Secret, the chart Secret keeps the webhook secret and admin token.
gitlab:
  aiUsername: review-agent
secrets:
  webhookSecret: ci-webhook-secret
  secretKeyRefs:
    gitlabToken:
      name: gitlab-token
      key: token
