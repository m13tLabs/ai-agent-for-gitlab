# Values chart-testing (`ct lint` / `ct install`) renders the chart with:
# the minimum the templates `required` to render.
gitlab:
  aiUsername: review-agent
secrets:
  gitlabToken: ci-gitlab-token
  webhookSecret: ci-webhook-secret
